[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "lcta-native"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
manifest-path = "Cargo.toml"
module-name = "_lcta_native"
bindings = "pyo3"
