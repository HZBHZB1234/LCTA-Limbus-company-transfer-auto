## 美化-配置文件

```json
[
    {"name": "**规则集", "desc": "用于做**",
    "rules": [
        {
            "name": "**规则",
            "desc": "替换...",
            "type": "re或者color(决定类型)",
            "aimFile": "正则表达式，从对应语言包为根目录的相对路径",
            "trigger": {
                "aim": "正则表达式，匹配键路径，使用.分割。trigger是可选的参数",
                "re": "正则表达式，匹配键内容，如果匹配到则将相应的trigger-aim添加处理"
            },
            "aim": "当不存在trigger时，逻辑如下：
            匹配当前文件的所有正则符合的内容添加处理。
            当存在trigger时，逻辑如下：
            对于每一个添加处理的trigger，将其路径进行相对处理后添加处理。
            相对路径语法如下：[back]表示回退路径，其余表示添加。.分割",
            "action": [
                {"from": "在re type情况下使用，正则表达式源",
                "to": "在re type情况下使用，正则表达式替换目标",},
                {"color类型action参数省略"}
            ]
        }
    ]}
]
```